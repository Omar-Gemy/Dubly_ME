# Dubly ME — Phase B & C: Speaker Layer + ASR
# Requirements for Windows Environment

torch==2.1.2
torchaudio==2.1.2
numpy<2.0
packaging
soundfile

# Phase B: Speaker Diarization (pyannote.audio)
# ⚠ SETUP REQUIRED before first use:
#   1. Create a Hugging Face account at https://huggingface.co
#   2. Accept the license at https://huggingface.co/pyannote/speaker-diarization-3.1
#   3. Accept the license at https://huggingface.co/pyannote/segmentation-3.0
#   4. Set your token:  $env:HF_TOKEN = "hf_your_token_here"  (PowerShell)
# pyannote.audio is NOT pinned here — whisperx==3.3.1 pulls the compatible
# 3.1.1 transitively (an explicit 4.x pin conflicts with it). The 3.1.1 ↔
# new-torchaudio break is handled at runtime by src/torchaudio_compat.py.

# Phase C: ASR / Transcription
# WhisperX handles full-file transcription + wav2vec2 forced alignment and
# pins compatible faster-whisper / ctranslate2 versions transitively — do NOT
# pin faster-whisper here (it conflicts with whisperx 3.3.1's own pin).
whisperx==3.3.1

# Phase D: Translation & Adaptation (offline LLM)
# Pinned so AutoAWQ (0.2.8) and WhisperX share one transformers build:
# transformers is held at 4.47.1 so transformers.activations.PytorchGELUTanh
# (imported by AutoAWQ) still exists — it was removed in 4.49.
transformers==4.47.1
accelerate>=1.1.0,<1.3.0
bitsandbytes>=0.44.0   # 4-bit quantization support
autoawq==0.2.8         # AWQ 4-bit inference for Qwen2.5-14B-Instruct-AWQ (GPU/Linux)
sentencepiece          # tokenizer backend for some models

# Phase E: Voice Cloning & TTS
TTS                # Coqui TTS — includes XTTS v2
soundfile          # audio I/O for WAV verification

# Phase F: Mix & Render (post-processing)
numpy<2.0           # audio timeline assembly & signal processing

# FFmpeg is a SYSTEM dependency — not installed via pip.
# Install it separately and ensure it is on PATH:
#    Windows : winget install FFmpeg
#    macOS   : brew install ffmpeg
#    Linux   : sudo apt install ffmpeg