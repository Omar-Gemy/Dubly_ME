# Dubly ME — Venv 2: LLM stack  (Phase D)
# ─────────────────────────────────────────────────────────────────
#   pip install -r requirements/llm.txt
#
# Phase D  translation.py   Qwen2.5-14B-Instruct-AWQ, local inference only.
#
# torch is intentionally UNPINNED: this venv accepts whatever CUDA build the
# platform provides (Colab ships its own). It is deliberately NOT held at the
# 2.1.2 pin requirements/asr-local.txt uses — that divergence is the entire
# reason these two stacks are isolated.

torch

# transformers is held at 4.47.1 so transformers.activations.PytorchGELUTanh
# (imported by AutoAWQ 0.2.8) still exists — it was removed in 4.49.
# This pin is load-bearing: do NOT loosen it in this venv.
transformers==4.47.1
accelerate>=1.1.0,<1.3.0
bitsandbytes>=0.44.0   # 8-bit fallback for non-prequantized checkpoints
autoawq==0.2.8         # AWQ 4-bit inference (GPU/Linux)
sentencepiece>=0.2.0   # tokenizer backend
protobuf>=4.25.0,<6.0
