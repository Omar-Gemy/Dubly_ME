# Dubly ME — Venv 4: Source separation  (Phase F0)
# ─────────────────────────────────────────────────────────────────
#   python -m venv .venv_demucs
#   .venv_demucs/bin/pip install -r requirements/demucs.txt
#
# Phase F0  source_separation.py   Demucs htdemucs two-stem separation.
#
# Demucs is torch-heavy and must not perturb the pinned ASR stack or the XTTS
# venv, so it gets its own environment. mix_render only ever reads the WAV
# artifacts this phase writes — never Demucs itself.

demucs
soundfile
numpy<2.0
scipy

# FFmpeg is a SYSTEM dependency (audio extraction to 44.1 kHz stereo).
